getgenv().aliases = {
    setclipboard = getgenv().setrbxclipboard, -- Alias for setrbxclipboard
    getdplayer = getgenv().getplayer,         -- Alias for getplayer
    localplayer = getgenv().getlocalplayer,   -- Alias for getlocalplayer
    listplayers = getgenv().getplayers,       -- Alias for getplayers
    runanimation = getgenv().runanimation,         -- Alias for runanimation
    playanimation = getgenv().playanimation        -- Alias for playanimation
}
